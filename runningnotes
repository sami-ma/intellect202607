create database intellect2026;

CREATE SCHEMA sami;

CREATE SCHEMA sami;

create table t1 (id int, name text);

show search_path

select current_user

set search_path = sami, public

create table t2 (id int, name text, address text);

create table public.t3 (name text)

ALTER USER <username> IN DATABASE <db name> SET search_path = myschema, public;


Auto-increment:

create table students (admn_no serial, student_name varchar(20), 
address varchar(100)) ;

select * from students;

insert into students (student_name, address)  values 
('Ravi', 'chennai');

select * from students;

insert into students (student_name, address)  
values ('Raj', 'Mumbai');

create table students2 (admn_no int generated always as identity
(start with 100 increment by 2 maxvalue 10000) , student_name varchar(20), 
address varchar(100)) ;

insert into students2 (student_name, address)  values 
('Ravi', 'chennai');

select * from students2;

insert into students2 (student_name, address)  
values ('Raj', 'Mumbai');

create table students2 (admn_no int generated always as identity
(start with 100 increment by 2 maxvalue 10000)  , student_name varchar(20), 
address varchar(100)) ;

create table students3 (admn_no int, student_name varchar(20), 
address varchar(100)) ;

create sequence sq_students3 as int increment by 2 
start with 1500 owned by students3.admn_no ;

insert into students3 values 
(nextval('sq_students3'), 'Ravi', 'chennai');

select * from students3;
 
insert into students3 values 
(nextval('sq_students3'), 'Raj', 'Bangalore');

select * from students3;

Constraints:

create table dept (dno int primary key, dname varchar(10) not null);

create table employee (
empno int primary key,
ename varchar(25) not null,
pan_no varchar(10) unique,
salary decimal(10,2) check (salary > 0),
deptno int references dept(dno)
);

Queries on Employee and department tables:

select * from emp;
select * from dept;
select * from salgrade;

select ename, job, sal from emp;
select sal, job from emp;
select * from emp;
select * from emp order by ename desc;
select * from emp order by ename;
select ename, job, sal from emp order by sal desc;
select ename, job, sal from emp order by sal desc, ename desc;
select * from emp limit 3;
select * from emp order by sal desc limit 3;
select ename, job from emp;
select ename, job from emp limit 2 offset 4;

select * from emp where ename = 'JONES';
select * from emp where ename in ('JONES', 'ALLEN');
select * from emp where ename like 'S%';
select * from emp where ename not like 'S%';

select * from emp where ename ~~ 'S%';
select * from emp where ename !~~ 'S%';

select job from emp;
select distinct job from emp;
select ename as "Employee Name", job as Designation from emp;
select ename as "Employee Name", job designation from emp;
select * from emp where ename = $$JONES$$;
-- date arithmetic
select now();
select now() + interval '10 days';
-- type casting
select date '2026-07-25' + interval '15 days';
select '2026-07-25'::date + interval '15 days';
-- to_char
select ename, job, to_char(hiredate, 'DAY') from emp;
select ename, job, to_char(hiredate, 'DY'), to_char(hiredate, 'MONTH') from emp;

-- aggregate function and group OPERATIONS

select sum(sal) from emp;
select avg(sal) from emp;
select min(sal), max(sal), count(*) from emp;

select job, min(sal), max(sal), count(*) from emp group by job;

select job, min(sal), max(sal), count(*) from emp group by job having count(*) >= 3;

select job, min(sal), max(sal), count(*), string_agg(ename, ',') from emp group by job;






